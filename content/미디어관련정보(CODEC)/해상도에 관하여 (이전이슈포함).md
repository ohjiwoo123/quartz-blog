### 이번에 있었던 발생한 문제







### 해상도를 미디어스트림으로부터 구하는 방법

송출 스트림으로부터 해상도를 구하는 방법은 2가지가 있다.

1. 첫 번째 방법은 OnMetaData에서 해상도를 읽는 방법 
- 아래는 와이어샤크로 RTMP패킷을 분석하여 OnMetaData 필드를 확인 한 것이다.
AMF 메세지로 감싸져있고, width와 height 부분의 key-value 값을 읽으면 된다.
![](https://velog.velcdn.com/images/ohjiwoo123/post/8a12b461-b854-4b27-8bed-1705531ee537/image.png)


2. 두 번째 방법은 sps(Sequence Parameter Sets)로 부터 읽는 것이다.
```
int SubWidthC;
int SubHeightC;

if (sps->chroma_format_idc == 0 && sps->separate_colour_plane_flag == 0) { //monochrome
    SubWidthC = SubHeightC = 0;
}
else if (sps->chroma_format_idc == 1 && sps->separate_colour_plane_flag == 0) { //4:2:0 
    SubWidthC = SubHeightC = 2;
}
else if (sps->chroma_format_idc == 2 && sps->separate_colour_plane_flag == 0) { //4:2:2 
    SubWidthC = 2;
    SubHeightC = 1;
}
else if (sps->chroma_format_idc == 3) { //4:4:4
    if (sps->separate_colour_plane_flag == 0) {
        SubWidthC = SubHeightC = 1;
    }
    else if (sps->separate_colour_plane_flag == 1) {
        SubWidthC = SubHeightC = 0;
    }
}

int PicWidthInMbs = sps->pic_width_in_mbs_minus1 + 1;

int PicHeightInMapUnits = sps->pic_height_in_map_units_minus1 + 1;
int FrameHeightInMbs = (2 - sps->frame_mbs_only_flag) * PicHeightInMapUnits;

int crop_left = 0;
int crop_right = 0;
int crop_top = 0;
int crop_bottom = 0;

if (sps->frame_cropping_flag) {
    crop_left = sps->frame_crop_left_offset;
    crop_right = sps->frame_crop_right_offset;
    crop_top = sps->frame_crop_top_offset;
    crop_bottom = sps->frame_crop_bottom_offset;
}

int width = PicWidthInMbs * 16 - SubWidthC * (crop_left + crop_right);
int height = FrameHeightInMbs * 16 - SubHeightC * (2 - sps->frame_mbs_only_flag) * (crop_top + crop_bottom);
```
내용을 참고하자.
https://stackoverflow.com/questions/31919054/h264-getting-frame-height-and-width-from-sequence-parameter-set-sps-nal-unit

그런데 지금 특이한 점은 우리팀의 코드 중에서
sar_width, sar_height을 포함해서 구하는 공식이 있다.
```
if (sps.vui_parameters_present_flag && sps.tVUIParameters.sar_width && sps.tVUIParameters.sar_height)
    {
        display_width = coded_width * sps.tVUIParameters.sar_width / sps.tVUIParameters.sar_height;
        printf(">> display_width(%d) coded_width(%d) sar_width(%d) sar_height(%d)\n", display_width, coded_width, sps.tVUIParameters.sar_width , sps.tVUIParameters.sar_height);
    }
```

찾아본 결과 따로 여기까지 분리해서 해상도를 표시할 필요는 없는 듯 함.
GPT 피셜,  
```
SPS
Width = 720
Height = 576
SAR = 16:15
```
이면 
```
Decoder
↓
720x576 출력

Renderer
↓
SAR 적용

768x576으로 표시
```
즉 Decoder와 Renderer 단계가 분리된다고 한다.


### SAR DAR PAR 

1. SAR = Storage Aspect Ratio (720x480인 경우 저장 종횡비는 3:2)
2. DAR = Display Aspect Ratio (화면의 종횡비)
3. PAR(SAR) = Pixel Aspect Ratio (픽셀의 종횡비) 
** 똑같이 Sample Aspect Ratio라고 부르기도 한다.

DAR = SAR * PAR 


결국에는 우리는 PAR(Pixel Aspect Ratio)을 구할 것이므로, Sample Aspect Ratio만 해상도를 구하는 것으로 하면 되겠다. 



### AV1의 경우

1. Sequence Header OBU 파싱

```
## OBU_SEQUENCE_HEADER

frame_width_bits_minus_1
frame_height_bits_minus_1

max_frame_width_minus_1
max_frame_height_minus_1

max_width  = max_frame_width_minus_1 + 1;
max_height = max_frame_height_minus_1 + 1;
```